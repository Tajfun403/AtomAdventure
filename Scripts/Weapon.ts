import { Actor } from "./Actor";


export class Weapon extends Actor {
    public ShootTowards(direction: [number, number]) {
        // TODO spawn a projectile actor and set its velocity to the direction the player is facing
        // TODO fill this thing
        // TODO spawn a projectile actor with simple AIController that will make it go towards the passed-in direction.
    }
}